---
title: split ()
module: string
sidebar_label: split
sidebar_class_name: function
hide_table_of_contents: true
---
import DocPage from '@site/src/components/DocPage';
import Spec from '@site/docs/api/string/Spec';

<DocPage fieldName="split" fieldType="function" spec={Spec} />

```scss
@use '@sass-fairy/string';

$str: 'The quick brown fox jumps over the lazy dog.';

@debug string.split($str);
  // 'The quick brown fox jumps over the lazy dog.'

@debug string.split($str, ' ');
  // 'The' 'quick' 'brown' 'fox' 'jumps' 'over' 'the' 'lazy' 'dog.'

@debug string.split($str, '', 3);
  // 'T' 'h' 'e'

@debug string.split($str, 'o', $separator: 'comma', $bracketed: true);
  // ['The quick br', 'wn f', 'x jumps ', 'ver the lazy d', 'g.']
```
