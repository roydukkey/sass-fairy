<!--
Copyright (c) roydukkey. All rights reserved.
Licensed under the MIT. See LICENSE file in the project root for full license information. -->

import { Fragment } from 'react';
import Parameters from './partials/Parameters.mdx';
import Signature from '../Signature';
import SignatureOutput from './partials/SignatureOutput.mdx';
import SignatureReturnValue from './partials/SignatureReturnValue.mdx';

<p>{props.field.description}</p>
<Fragment>{props.children}</Fragment>

<Fragment>
  {props.field.parameters &&
    <Parameters {...props} />
  }
</Fragment>

<Fragment>
  {props.field.return &&
    <SignatureReturnValue spec={props.spec} fieldName={props.fieldName} />
  }
</Fragment>

<Fragment>
  {props.field.output &&
    <SignatureOutput spec={props.spec} fieldName={props.fieldName} />
  }
</Fragment>

## Example
